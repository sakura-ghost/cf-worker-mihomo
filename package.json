{
	"name": "cf-worker-mihomo",
	"version": "1.2.0",
	"private": true,
	"license": "MIT",
	"scripts": {
		"deploy:workers": "node worker_esbuild.js && wrangler deploy",
		"deploy": "vercel --prod",
		"dev:workers": "node worker_esbuild.js && wrangler dev",
		"dev": "vercel dev",
		"build:workers": "node worker_esbuild.js",
		"build": "vercel build",
		"start:workers": "node worker_esbuild.js && wrangler dev",
		"start": "vercel dev",
		"lint": "prettier --write src/*"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "0.8.47",
		"esbuild": "^0.25.8",
		"vercel": "^44.2.7",
		"wrangler": "^4.22.0"
	},
	"dependencies": {
		"@koa/router": "^13.1.0",
		"fs": "^0.0.1-security",
		"koa": "^3.0.0",
		"node-fetch": "^3.3.2",
		"yaml": "^2.8.0"
	}
}