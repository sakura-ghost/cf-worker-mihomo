{
	"name": "cf-worker-mihomo",
	"version": "1.2.0",
	"private": true,
	"license": "MIT",
	"scripts": {
		"deploy:workers": "node esbuild.js && wrangler deploy",
		"deploy": "vercel --prod",
		"dev:workers": "node esbuild.js && wrangler dev",
		"dev": "vercel dev",
		"build": "node esbuild.js",
		"start:workers": "node esbuild.js && wrangler dev",
		"start": "vercel dev",
		"lint": "prettier --write \"**/*.js\""
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "0.8.47",
		"esbuild": "^0.25.8",
		"vercel": "^44.2.7",
		"wrangler": "^4.22.0"
	},
	"dependencies": {
		"prettier": "^3.6.2",
		"yaml": "^2.8.0"
	}
}
