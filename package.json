{
	"name": "cf-worker-mihomo",
	"version": "1.2.0",
	"private": true,
	"type": "module",
	"license": "MIT",
	"scripts": {
		"deploy:workers": "npx wrangler deploy",
		"deploy": "npx vercel --prod",
		"dev:workers": "npx wrangler dev",
		"dev": "npx vercel dev",
		"build:workers": "npx wrangler build",
		"build": "npx vercel build",
		"start:workers": "npx wrangler dev",
		"start": "npx vercel dev"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "0.8.47",
		"wrangler": "^4.22.0",
		"vercel": "^44.2.7"
	},
	"dependencies": {
		"@koa/router": "^13.1.0",
		"fs": "^0.0.1-security",
		"koa": "^3.0.0",
		"node-fetch": "^3.3.2",
		"yaml": "^2.8.0"
	}
}